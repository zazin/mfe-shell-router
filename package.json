{
  "name": "mfe-shell-router",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "check": "npm run typecheck",
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.84.1"
  }
}
